language: node_js
node_js: stable
services: 
  - mongodb

before_script: 
  - npm install lab -g
  - sleep 5
  - mongo sljRN --eval 'db.createCollection("usergenerates");'
  - mongo sljRN --eval 'db.usergenerates.save({_id:"User", next:1});'

script:
  - npm run test